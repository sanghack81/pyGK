# Unreleased

- support SciPy 1.14 and later in random-walk kernels
- support arbitrary NetworkX node identifiers and unlabeled `KGraph` inputs
- validate graphlet size and sampling parameters before computation
- migrate packaging and CI to `pyproject.toml` and GitHub Actions

# Release 1.0.1

- fix errors in creating a matrix in `labeled_shortest_path_kernel`
